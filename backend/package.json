{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.mjs --external:'@aws-sdk/*'"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1105.0",
    "@aws-sdk/client-ssm": "^3.1105.0",
    "@aws-sdk/lib-dynamodb": "^3.1105.0",
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.1"
  }
}
