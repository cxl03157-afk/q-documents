# 現在地

**セッション開始時はこの1枚を読む。** 作業の区切りごとに更新する。

| 項目 | 値 |
| --- | --- |
| 最終更新 | 2026/7/31 |
| 現在地 | **週1・1日目 完了**（`docs/phase-roadmap.md`） |
| 提出期限 | 2026/8/24（残り24日） |
| 作業ブランチ | `docs/issue-1-project-rules` |
| Issue / PR | #1 プロジェクト運用ルールの整備とIP制限の技術検証 ／ **PR #2**（`Closes #1`） |
| 受け入れ基準の消化 | **1 / 25**（HTTP→HTTPSリダイレクトを検証中に確認） |

---

## NextAction（次の1アクション）

**週1・2日目（8/1）: モック作成の着手** — Vite + vanilla TS を初期化し、`shared/types.ts` を定義する。
続けて共通ヘッダー・ハッシュルーティング・S-1（一覧と5条件の絞り込み）まで。

---

## 作業できない日と修正後の日程

**8/4（火）・8/5（水）は不在のため作業できない。** この2日を織り込んだ日程は以下。

| 日 | 作業 |
| --- | --- |
| 8/1（土） | Vite+TS初期化、`shared/types.ts`、共通ヘッダー、ハッシュルーティング、**S-1** |
| 8/2（日） | **S-2〜S-4**、ロック/解除の状態管理・30分自動ロック |
| 8/3（月） | **S-5〜S-7**、関連文書パネル、CSV、**モックのレビューと設計反映** |
| 8/4・8/5 | **✗ 不在**（可能なら書いたコードの通読のみ） |
| 8/6（木） | **Terraform ストレージ層**（S3×2 / DynamoDB×2 / IAM / SSM / Budgets） |
| 8/7（金） | **Terraform 公開層**、Hello World疎通、**社外IP拒否の実測**、デプロイスクリプト |
| 8/8（土） | `shared/types.ts` 確定、文書番号パースの純粋関数＋Vitest |
| 8/9（日） | 合言葉 F-18 |
| 8/10（月） | 採番 F-02 / F-07 / F-03 |
| 8/11（火） | マスタCRUD F-10、一覧API接続 F-05 |
| 8/12（水） | 署名付きURL・S3イベントの試作 |

**週1のTerraform（6〜7日目）を週2の先頭に移した。** モックは7画面すべて作る方針を維持する。

### この日程のリスク

**週2（8/6〜8/12）は7日に7日分で余裕がゼロ。** 1日でも詰まると週3（最大の山場）に食い込む。
**週3に遅れを持ち込まない**ことを優先し、遅れた場合は次の順序で削る。

1. S-6 マスタ管理画面を最小限にする（APIは作り、画面は簡素に）
2. CSV出力（F-08）を週3へ後送する
3. 署名付きURL・S3イベントの試作を圧縮する（**最後の手段**。週3が重くなる）

**週1の受け入れ基準3項目**（HTTP拒否 / 社外IPから到達不可 / S3のURL直叩き不可）の確認は 8/7 にずれる。

---

## 直近の決定事項

| 日付 | 決定 |
| --- | --- |
| 7/31 | **IP制限は WAF 不要と確定。** CloudFront Functions と API Gateway リソースポリシーの両方で実測合格（`docs/ip-restriction-verification.md`）。判断期限 8/5 を待たず確定 |
| 7/31 | **CloudFront は `is_ipv6_enabled = false` にする。** 既定のIPv6有効だと `viewer.ip` にIPv6が入り、社内IPv4の許可リストが効かない |
| 7/31 | **API Gateway は REGIONAL + IPアドレスタイプ IPv4** |
| 7/31 | リポジトリは **public**。社内グローバルIPは `.gitignore` の `*.tfvars` で機械的に除外し、コミットしない |
| 7/31 | ブランチ保護: PR必須・必要承認数0・管理者にも適用・force push禁止。**必須ステータスチェックは週2でCI作成時に追加する** |
| 7/31 | **開発の目的は「動作すること」と「内容を自分で説明できること」の2つで、優先度は同じ。** 両立できないときに削るのは機能 |
| 7/31 | **「社内IP」は自宅回線のグローバルIPで代替する。** 題材は前職の課題であり、現在その社内ネットワークにはアクセスできない。**拒否側の実証はスマホのテザリング**（別のグローバルIP）で行う。許可IPは Terraform 変数（CIDRリスト）で外出しし、社内IPに差し替えるだけで本番運用できる構成にする（`docs/ip-restriction-verification.md`） |
| 7/31 | **元勤務先が特定できる情報は入れない。** public リポジトリのため、社名・部署名・実在の製品コード・工程名は使わない |

---

## 未解決

| 項目 | 期限・扱い |
| --- | --- |
| ~~検証用AWSリソースの削除~~ | **7/31 完了**（3件とも削除済み） |
| CloudFront のWAF無料枠 | 「プランに含まれ追加料金なし」と表示された。許可リスト型のカスタムIPルールが無料枠に含まれるかは未確認。**今回はWAF不要と確定したため実害なし。参考情報として保留** |
| `terraform destroy` とプラン契約 | ディストリビューション削除にプラン解約が必要な挙動が、Terraform で扱えるか未確認 |
| ローカル Node v25 と Lambda `nodejs22.x` の差 | 週1・2日目の初期化時に `.nvmrc`(22) を置くか判断する |

---

## 今日やったこと（7/31）

- git init → GitHub リポジトリ `q-documents`（public）作成 → ブランチ保護（実測で拒否を確認）
- `CLAUDE.md` 作成（採番ルール・SK組み立て・状態5値・ストレージクラス・責務分担・Git/デプロイ運用・ゲート表）
- `docs/phase-gates.md` / `docs/agent-prompts.md` を FitLog から移植、本書を新規作成
- **IP制限の技術検証（A〜E 全項目合格）**
