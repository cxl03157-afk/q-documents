@docs/context.md

# Q-documents 開発ルール

品質文書管理アプリ Q-documents の開発で、毎回参照する不変条件を置く。
**設計内容の再掲はしない。** 詳細は各設計ドキュメントを正とする。

冒頭の `@docs/context.md` はインポート指定で、セッション開始時に現在地（週・日・NextAction）も
一緒に読み込まれる。**`docs/context.md` が古いと毎回古い情報が混入するため、作業の区切りごとに更新する**
（`docs/phase-gates.md` の終了時チェックに含めてある）。

---

## 0. 言語と確認の粒度

### 応答は日本語で行う

> **この設定はコンテキストが圧縮された後も維持する。**

### 確認の粒度（どこで止まるか）

| レベル | 扱い | 該当する操作 |
| --- | --- | --- |
| **L0: 確認不要** | そのまま実行してよい | ファイルの読み取り・検索、`git status` / `git diff` / `git log`、ブランチの作成と切り替え、テスト・lint・buildの実行、AWSの**読み取り専用**コマンド（`describe` / `get` / `list`）、ドキュメントの下書き提示 |
| **L1: 説明してから実行** | 実行前に何をするか1〜2行で述べる | ファイルの新規作成・編集、Issue作成、PR作成 |
| **L2: 承認が必要** | **止まって承認を得る** | **コードを書くこと**（§1 実装ゲート）、コミット、**push**、マージ、AWSリソースの作成・変更・削除、`terraform apply` / `destroy`、設計ドキュメントの変更 |
| **L3: 禁止** | ツール側で拒否される | `.claude/settings.json` の `deny` を参照（`rm` / `mv` / `git clean` / force push / `git reset --hard` など） |

**L0 で逐一確認を取らない。** 調べれば分かることを質問に変えると、26日という期間では確認の往復だけで時間が尽きる。
逆に **L2 は必ず止まる。** 特にコードを書く前と push の前は例外を作らない。

**設計ドキュメント（`docs/` の設計7点）の修正はユーザー自身が行う。**
私は「どこを・何に・なぜ」を提示するだけで、勝手に編集しない
（`CLAUDE.md` / `docs/context.md` / 検証記録など、運用のために私が作ったファイルは対象外）。

---

## 1. 目的と実装ゲート

**目的は2つあり、優先度は同じ。どちらか一方を犠牲にしない。**


| #   | 目的                | 理由                                                                       |
| --- | ----------------- | ------------------------------------------------------------------------ |
| 1   | **動作すること**        | **コンテストへの提出物であり、動かなければ審査の対象にならない**（提出期限 2026/8/24）                       |
| 2   | **内容を自分で説明できること** | コンテストに限らず作ったものには説明を求められることがある。AIは活用するが、自分で内容を説明できる、作ったものに対して責任が取れることが必要。 |


技術選定はこの2つから逆算している
（React を採用せず vanilla TS にしたのは「学習対象を TypeScript のみに絞る」ため）。

### 両立できないときに削るのは「機能」

期限が迫ったときに、**理解を飛ばして動かす**のも、**動かないまま説明だけできる**状態にするのも不可。
**削る対象は常に機能**とする。

- **コード量の目安は全体で1,800〜3,000行。** 超え始めたら機能を削るか設計を見直す合図
- ストレッチ機能（F-14〜F-17）は着手しない前提。コア機能に集中する
- 受け入れ基準25項目（要件定義書 §7）を満たすことが「動作すること」の定義



### 実装ゲート（必須）

> **実装前に、何をどう書くかを日本語で説明し、承認を得てから書く。**

- コードを見せてから説明する順序にしない
- 「動いたからOK」で先に進めない。説明できない箇所は止めて確認する
- ただし**説明のために手が止まり続けるのも避ける**。判断に迷ったら、動かして確かめてから説明を詰める

---



## 2. 正の所在


| 対象                  | 正とするファイル                              |
| ------------------- | ------------------------------------- |
| 要件・受け入れ基準           | `docs/要件定義書.md`                       |
| 技術選定と不採用の理由         | `docs/tech-stack.md`                  |
| キー設計・採番ルール          | `docs/DynamoDBテーブル設計.md`              |
| APIインターフェース         | `docs/API.md`                         |
| 画面仕様                | `docs/screens.md`                     |
| 工程・PR運用             | `docs/phase-roadmap.md`               |
| IP制限の実測結果と申し送り      | `docs/ip-restriction-verification.md` |
| 現在地（週・日・NextAction） | `docs/context.md`                     |


**迷ったら上を見る。CLAUDE.md に書き写さない。** 二重管理は必ずずれる。

Terraform・CI/CD の一般的な知見はグローバル `~/.claude/CLAUDE.md` にあるため、ここには重複して書かない。

---



## 3. 文書番号の採番ルール


| 文書種類  | 形式                       | 例                    |
| ----- | ------------------------ | -------------------- |
| PFMEA | `文書種類_製品コード_Rev(2桁)`     | `Q001_P-0001_01`     |
| QC工程表 | `文書種類_製品コード_Rev(2桁)`     | `Q005_P-0001_01`     |
| 作業指示書 | `製品コード_工程番号_工程名_Rev(2桁)` | `P-0001_K001_工程1_02` |


- **共通コード**（製品によらない作業）は、製品コードの位置に `COM001` のような記号を置く。
`COM001_K001_工程1_01` のようになる。**工程単位の文書（作業指示書）にしか存在しない**。
判定はマスタの `isCommon` で行い、**コードの形式（`P-` で始まる等）では判定しない**
- **リビジョンは2桁ゼロ埋め**（`1` ではなく `01`）
- ファイル名 = **文書番号 + 拡張子**（`Q001_P-0001_01.pdf` / `Q001_P-0001_01.xlsx`）
- 許容する拡張子は `.pdf` と `.xlsx` のみ。**大文字小文字は区別しない**
- 拡張子でファイル種別を判定し、`.pdf` は S3キー(PDF)、`.xlsx` は S3キー(Excel) に記録する

---



## 4. SK の組み立て方

DynamoDB 文書台帳: **PK = 製品コード** / **SK =** `文書ID#リビジョン`

**文書ID = 文書番号から末尾の** `_Rev` **を除いた部分。**


| 文書番号                 | 文書ID              | SK                   |
| -------------------- | ----------------- | -------------------- |
| `Q001_P-0001_01`     | `Q001_P-0001`     | `Q001_P-0001#01`     |
| `P-0001_K001_工程1_02` | `P-0001_K001_工程1` | `P-0001_K001_工程1#02` |




### パースの実装方針

**末尾の** `_数字2桁` **をリビジョンとして切り出す**（`/^(.+)_(\d{2})$/`）。

`_` で分割して要素数から判定する方式は採らない。**工程名に** `_` **が含まれた瞬間に壊れる**ため
（例: `P-0001_K001_組立_仮_02`）。末尾から切る方式ならこのケースにも耐える。

### 区切り文字の制約


| 文字  | 用途         | 制約                      |
| --- | ---------- | ----------------------- |
| `_` | 文書番号内の区切り  | 工程名に含まれても可（上記パース方式のため）  |
| `#` | SK の結合     | **工程名に含めてはいけない。SKが壊れる** |
| `/` | S3キー・URLパス | **工程名に含めてはいけない**        |


`#` と `/` はマスタ登録時のバリデーションで弾く（実装は週2のマスタCRUD）。

---



## 5. 状態5値と遷移条件

```
ファイル未登録 ──→ 一部登録 ──→ 最新 ──→ 旧版
      │              │           │         │
      └──────────────┴───────────┴─────────┴──→ 削除済み（論理削除）
```


| 状態        | いつ入るか                                                                 |
| --------- | --------------------------------------------------------------------- |
| `ファイル未登録` | `POST /documents` または `POST /documents/{docNo}/revisions` で台帳に記録された直後 |
| `一部登録`    | PDF・Excel の**片方だけ**S3キーが記録された状態                                       |
| `最新`      | **両方**のS3キーが揃った                                                       |
| `旧版`      | 同一文書IDの**新しいRevが「最新」になった**とき                                          |
| `削除済み`    | `DELETE /documents/{docNo}`（論理削除）。どの状態からでも遷移する                        |




### 破ってはいけない規律

- **逆方向の遷移はない。** 一度「最新」になったものが「一部登録」に戻ることはない
- **状態を書き換えられるのは非同期Lambdaだけ**（`削除済み` を除く）。
画面・同期APIは状態を書き換えない。S-7（修正画面）でも状態は編集不可
— 手で変えられると、ファイルが存在しないのに「最新」になり、F-04のアーカイブ判定が狂う
- **二重実行を前提に書く。** PDF・Excel をほぼ同時に送ると非同期Lambdaが並行起動し、
さらに S3 イベントは at-least-once なので同じイベントが2回届くことがある。
**条件付き書き込みによる先勝ち**で冪等性を確保する
- `一部登録` は通常フローでは発生しない例外状態（保存エラーで片方だけ残った場合）。
**復旧可能な中間状態として扱う** — 登録済みの種別は再登録できないが、
**未登録の種別だけを後から追加できる**（要件定義書 F-01）。両方揃えば「最新」へ進む。
エクセルは10〜50MB、PDFはその数分の一なので、片方の失敗で両方を送り直させない
- **論理削除して発行し直すのは、復旧できない場合の最終手段。**
正しいファイル名で中身の違うファイルを登録し終えた場合がこれにあたる（その種別は差し替えられない）。
**このため、採番時の重複チェックは `削除済み` を数えない。** 数えると発行し直せず、復旧手段が消える。
同じ文書番号で作り直すと、同じキーへの書き込みになるので削除済みのレコードは置き換わる
- **論理削除は取り消せない。** 削除前の状態を保存していないうえ、状態の再計算は非同期Lambdaの仕事のため。
消してしまった場合は上記のとおり発行し直す。削除済みは解除時に状態で選べば一覧で確認できる（エクセルのみ取得可）

---



## 6. ストレージクラスの割り当て


| 対象              | クラス                       | いつ決まるか                                        |
| --------------- | ------------------------- | --------------------------------------------- |
| 最新版PDF          | Standard                  | アップロード時の既定（指定しない）                             |
| 最新版Excel        | Standard-IA               | **アップロード時**。署名条件に `x-amz-storage-class` を含める  |
| 旧版（PDF・Excel両方） | Glacier Instant Retrieval | 「旧版」遷移時に非同期Lambdaが `CopyObject`               |


- **最新版Excel を Standard に置いてから移し替えることはしない。** 置き先は文書種別だけで決まり、
アップロードの時点で分からない情報は何もない。署名条件に入れれば10〜50MBのコピーが1往復まるごと消え、
**非同期Lambdaが自分の出したコピーのイベントで自分を呼び戻す再帰の経路も1本減る**
- **`CopyObject` を使うのは旧版のGlacier IR行きだけ。同一キーへコピーしてクラスのみ変更し、台帳のS3キー属性は書き換えない**
- **コピー前に現在のクラスを確認する**（`HeadObject`）。Glacier IR は90日の最低保管期間があり、
同じキーへ置き直すたびに early deletion 料金が乗る。確認は再帰を1ホップで止める役目も兼ねる
- 全てStandardに置くと400GBで月額約1,500円となり、コスト要件（月1,000円以内）を超過する。
**このライフサイクル制御は必須であって最適化ではない**

---



## 7. 役割の責務分担


| 層                    | 責務                                       | やらないこと                                 |
| -------------------- | ---------------------------------------- | -------------------------------------- |
| CloudFront Functions | **画面のIP制限のみ**                            | SPAルーティング等の別役割を持たせない（ハッシュルーティングを選んだ理由） |
| API Gateway リソースポリシー | APIのIP制限                                 | —                                      |
| 同期API Lambda         | 合言葉の検証・トークン発行／採番／照合／Rev更新／台帳記録／署名付きURL発行 | 状態遷移（新規作成時の「ファイル未登録」記録を除く）             |
| 非同期Lambda（S3イベント）    | S3キーの記録／状態遷移／ストレージクラス変更                  | クライアントから直接呼ばれるエンドポイントを持たない             |
| 画面（frontend）         | 絞り込み・CSV出力・関連文書の導出・表示制御                  | **検証の最終判断**（早期通知のために行うが、正はサーバー側）       |
| `shared/types.ts`    | 型の単一の正                                   | backend・frontend で型を二重定義しない            |




### 検証の二重化ルール

ファイル名・拡張子・文書番号の検証は**画面側とサーバー側の両方**で行う。

- 画面側 = 誤りを早く知らせるため
- **サーバー側 = 正。** 画面の制御は開発者ツールから回避できる
- **サーバー側の検証を省略しない。** 省略すると、エクセル欄にPDFを入れられた場合に
S3の `.xlsx` キーにPDFの実体が入り、非同期Lambdaが「両方揃った」と判断して「最新」にする。
台帳上は正常に見えるため、後日エクセルを開くまで誤りに気づけない

---



## 8. セキュリティの不変条件

**これを破る変更は、理由があっても一度止めて確認する。**

1. **合言葉は SSM Parameter Store（SecureString）** — コード・Terraform state・リポジトリに平文で置かない
2. **社内グローバルIPをコミットしない** — public リポジトリのため。非コミットの tfvars か SSM で渡す（`.gitignore` の `*.tfvars` で機械的にも防ぐ）
3. **署名付きURLの有効期限は15分以内**
4. **アップロード用URLは** `Content-Type` **を署名条件に含め、**`content-length-range` **でサイズ上限を強制する**
5. **S3 は Block Public Access + OAC 経由のみ。** 直接アクセスさせない
6. **合言葉の検証はサーバー側で行う。** 画面側だけの制御にしない
7. **エクセル・旧版へのアクセス時は、氏名・文書番号・ファイル種別・日時を CloudWatch Logs に記録する**
8. Lambda実行ロールは対象リソースとアクションを限定した最小権限とする
9. **CloudFront は** `is_ipv6_enabled = false`**／API Gateway は REGIONAL + IPv4** — IPv6経路を開けると許可リストが機能しない（`docs/ip-restriction-verification.md` の実測結果）
10. **元勤務先が特定できる情報を入れない** — 題材は前職の課題。public リポジトリのため、社名・部署名・実在の製品コード・工程名は使わない

> **「社内IP」の実体について**: 現在その社内ネットワークにはアクセスできないため、
> 開発者の自宅回線のIPを社内IPに見立てて実証する。拒否側の確認はスマホのテザリングで行う。
> **許可IPは Terraform 変数（CIDRリスト）で外から渡し、差し替えるだけで本番運用できる構成にする。**
> 詳細と理由は `docs/ip-restriction-verification.md`。

---



## 9. Git 安全運用



### ブランチとコミット


| 種別       | ブランチ名                      |
| -------- | -------------------------- |
| 機能追加     | `feature/issue-{番号}-{内容}`  |
| バグ修正     | `fix/issue-{番号}-{内容}`      |
| ドキュメント   | `docs/issue-{番号}-{内容}`     |
| リファクタリング | `refactor/issue-{番号}-{内容}` |


コミットメッセージは Conventional Commits（`<type>: <概要>`）。
type は `feat` / `fix` / `docs` / `refactor` / `test` / `chore`。

**main への直接 push は禁止**（ブランチ保護で強制。管理者にも適用）。

### stash 衝突の防止

- 退避の第一選択は stash ではなく**作業ブランチへの WIP コミット**
- `git stash pop` **は使わない。** 展開は `git stash branch temp/<name> stash@{n}` で別ブランチに出す
- stash を使う場合は必ずメッセージ付き（`git stash push -m "wip: ..."`）
- stash 適用前に `git status --porcelain` が空であることを確認する



### push 前チェックリスト（すべての push に適用）

1. `git status` を実行する
2. 以下の形式で報告する
  ```
   - git status: 実行済み
   - ブランチ: <branch名>
   - 未コミット変更: あり / なし
   - push 先: origin/<branch名>
   - push 対象コミット: <hash> <message>
  ```
3. **明示承認を得てから push する**
4. 未コミット変更がある場合は原則 push しない



### 毎日 push する

**1日の終わりに最低1回は push する。** PRの準備ができていなくてもよい。
単独開発でマシンが1台なら、ローカルリポジトリが単一障害点になる。目的はバックアップ。

---



## 10. デプロイ運用

- `aws s3 sync` **は** `--dryrun` **で確認してから本実行する**
- `--delete` **を使う場合は** `--exclude` **を必ず付ける**
- CloudFront のキャッシュ削除は `aws cloudfront create-invalidation --paths "/*"`
- デプロイは `scripts/` のスクリプト経由で行う（手順書に書くより守られる）



### lint + test だけで CI グリーン相当と判断しない

`npm run build` **でしか検出できない型エラーがある。** コミット前は lint → test → **build** の順で通す。

---



## 11. PR とマージの順序


| 種類          | 順序                                         |
| ----------- | ------------------------------------------ |
| コードのPR      | セルフレビュー → **マージ** → デプロイ                   |
| **インフラのPR** | ブランチ上で `terraform apply` → **成功を確認** → マージ |


インフラは適用してみないと通るか分からないため、**apply が通ったコードだけを main に入れる**。

**「途中まで実装した状態」を main に入れない。** マージのたびに main が動く状態を保てば、
どこで壊れたかが常に1つ前のマージまで絞り込める。

---



## 12. 作業の進め方（ゲート表）


| ステップ                 | 内容                                                                                      |
| -------------------- | --------------------------------------------------------------------------------------- |
| 計画                   | 対象範囲の実装順序・リスク・成果物を提示する                                                                  |
| **日本語での実装説明（必須ゲート）** | **何をどう書くかを日本語で説明し、承認を得る。承認前にコードを書かない**                                                  |
| Issue                | **機能のまとまり単位**で立てる（週4〜6本）。PR 1本ごとには立てない。案を提示して承認を得てから発行する                                |
| 実装                   | 承認後に実装。**コミット前に diff とメッセージ案を提示して承認を得る**                                                |
| 仕様変更ゲート              | APIパス・リクエスト/レスポンス・DBスキーマ・認証認可・エラー仕様を承認済み計画から変更する場合は作業を止め、変更理由・影響範囲・更新対象ドキュメントを提示して承認を得る |
| テスト報告                | 実行したコマンドと結果を提示する。**未実施の項目は「未実施」と明記する**                                                  |
| セルフレビュー              | `/code-review` で差分を確認し、指摘を解消する                                                          |
| PR作成                 | `git status` がクリーンであることを確認 → PR案を提示して承認を得てから作成（本文に `Closes #<issue>`）                   |
| マージ                  | CI グリーン確認後にマージ                                                                          |


各週・各日の開始と終了では `docs/phase-gates.md` のチェックリストを使う。
`docs/context.md` は作業の区切りごとに更新する。

### テストを書く範囲

全体網羅はしない。**最も間違えやすい純粋関数に絞る。**

1. 文書番号のパース・生成（3種別、SK組み立て、2桁ゼロ埋め）
2. 状態遷移の判定（両ファイルが揃ったか、前リビジョンをどう扱うか）

それ以外は要件定義書 §7 の受け入れ基準25項目を手動で確認する。
**受け入れ基準を週4に全部残さない。** 週4は初回確認ではなく回帰確認とする。